% --- Russian support & typography ---
\usepackage[utf8]{inputenc}
\usepackage[T2A]{fontenc}
\usepackage[russian]{babel}
\usepackage{csquotes}
\usepackage{indentfirst}
\usepackage[protrusion=true, expansion=false]{microtype}
\usepackage{titlesec}
\usepackage{lipsum}

\usepackage{comment}

% --- Page layout & spacing ---
\usepackage{geometry}
\geometry{
  top=20mm, bottom=20mm,
  left=30mm, right=15mm,
  headheight=15pt
}

\renewcommand{\unnumberedChapter}[1]{%
  \chapter*{#1}%
  \addcontentsline{toc}{chapter}{#1}%
}

% ─── Chapter: 16pt, centered, numbered inline ───
\titleformat{\chapter}[display]
  {\normalfont\centering\bfseries}          % <format>
  {}                                        % <label> (empty → removes separate block)
  {0pt}                                     % <sep>
  {\fontsize{16pt}{20pt}\selectfont\thechapter\quad} % <before-code>

% ─── Unnumbered chapters (\chapter*{...}) ───
\titleformat{name=\chapter,numberless}[display]
  {\normalfont\centering\bfseries}
  {}
  {0pt}
  {\fontsize{16pt}{20pt}\selectfont}

% ─── Section: 14pt, left-aligned, numbered ───
\titleformat{\section}
  {\normalfont\bfseries}
  {\thesection}
  {1em}
  {\fontsize{14pt}{17pt}\selectfont}

\usepackage{setspace}
\onehalfspacing

% --- Math & graphics ---
\usepackage{amsmath, amssymb}
\usepackage{graphicx}
\graphicspath{{images/}}

% --- Hyperlinks & bookmarks ---
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=blue,
  citecolor=black,
  pdftitle={Master thesis},
  pdfauthor={Roman Gorbunov}
}

% --- Bibliography (ГОСТ стиль) ---
\usepackage[style=gost-numeric, sorting=none, backend=biber]{biblatex}
\addbibresource{references.bib}