% --- Russian support & typography ---
\usepackage[T2A]{fontenc}      % Cyrillic font encoding
%\usepackage[utf8]{inputenc}    % UTF-8 source input
\usepackage{tempora}   % Times New Roman for main text.
\usepackage{newtxmath}   % Times New Roman for math symbols.
\usepackage{cmap}              % Makes Cyrillic text searchable/copyable in PDF
\usepackage[russian]{babel}    % Russian hyphenation & captions
\usepackage[autostyle=true, style=russian]{csquotes}

%\usepackage{csquotes}
\usepackage{indentfirst}
\usepackage[protrusion=true, expansion=false]{microtype}
\usepackage{float}
\usepackage{tocloft}
\usepackage{titlesec}

% TOC Title: "Содержание" & Centered.
\addto\captionsrussian{\renewcommand{\contentsname}{Содержание}}
\makeatletter
\renewcommand{\@cftmaketoctitle}{%
  \chapter*{\contentsname}%
  \@mkboth{\contentsname}{\contentsname}%
  \@starttoc{toc}%
}
\makeatother
% Removes bold (makes normal weight) from chapter titles in ToC.
\renewcommand{\cftchapfont}{\normalfont}
% Removes bold from chapter page numbers.
\renewcommand{\cftchappagefont}{\normalfont}
% Enables dots for all chapters in TOC.
\renewcommand{\cftchapdotsep}{\cftdotsep}  
% Forces identical baseline & style.
\renewcommand{\cftchapleader}{\normalfont\cftdotfill{\cftdotsep}}
% Chapter → Chapter/Section gap.
\setlength{\cftbeforechapskip}{0pt}
% Section → Section gap.
\setlength{\cftbeforesecskip}{0pt}
% Subsection → Subsection gap.
\setlength{\cftbeforesubsecskip}{0pt}

\usepackage{comment}

\usepackage{setspace}
\onehalfspacing

\usepackage{enumitem}
\setlist[itemize]{
  itemsep=0.0em, parsep=0pt,
  topsep=0pt, partopsep=0pt
}
\setlist[enumerate]{
  itemsep=0.0em, parsep=0pt,
  topsep=0pt, partopsep=0pt
}

\usepackage{caption}
\captionsetup[figure]{name=Рисунок, labelsep=period}
\captionsetup[table]{name=Таблица, labelsep=period}

% --- Page layout & spacing ---
\usepackage{geometry}
\geometry{
  top=20mm, bottom=20mm,
  left=30mm, right=15mm,
  headheight=15pt
}

% Chapter: size (pt), centered, numbered inline.
\titleformat{\chapter}[display]
  {\normalfont\centering\bfseries\fontsize{16pt}{20pt}\selectfont} % <format>
  {}                                        % <label> (empty → removes separate block)
  {0pt}                                     % <sep>
  {\thechapter\quad} % <before-code>

% Unnumbered chapters (\chapter*{...}).
\titleformat{name=\chapter,numberless}[display]
  {\normalfont\centering\bfseries\fontsize{16pt}{20pt}\selectfont}
  {}
  {0pt}
  {}

% Section and Subsection: 14pt, left-aligned, numbered.
\titleformat{\section}
  {\normalfont\bfseries\fontsize{14pt}{17pt}\selectfont}
  {\thesection}
  {1em}
  {}

\titleformat{\subsection}
  {\normalfont\bfseries\fontsize{14pt}{17pt}\selectfont}
  {\thesubsection}
  {1em}
  {}

% Calculate width for longest appendix label: "Приложение Я ".
\newlength{\appchapnumwidth}
\AtBeginDocument{%
  \settowidth{\appchapnumwidth}{\normalfont\cftchapfont Приложение~Я.}
}
\addtolength{\appchapnumwidth}{0.0em}

\makeatletter
\newcommand{\cyralph}[1]{%
  \ifcase#1\or А\or Б\or В\or Г\or Д\or Е\or Ж\or З\or И\or К\or Л\or М\or Н\or О\or П\or Р\or С\or Т\or У\or Ф\or Х\or Ц\or Ч\or Ш\or Щ\or Ъ\or Ы\or Ь\or Э\or Ю\or Я\else ??\fi}
\makeatother

\newcommand{\setupappendix}{%
  \setcounter{chapter}{0}%
  \renewcommand{\chaptername}{Приложение}%
  \renewcommand{\thechapter}{\cyralph{\value{chapter}}}%
  \renewcommand{\theHchapter}{app\thechapter}%
  \addtocontents{toc}{%
    \protect\renewcommand{\protect\cftchappresnum}{Приложение~}%
    \protect\renewcommand{\protect\cftchapaftersnum}{.\ }% Period + space in TOC.
    \protect\setlength{\protect\cftchapnumwidth}{\the\appchapnumwidth}%
  }%
  
  % Counters reset per appendix.
  \counterwithin*{section}{chapter}%
  \counterwithin*{figure}{chapter}%
  \counterwithin*{table}{chapter}%
  
  % Period appears ONLY in the printed heading.
  \titleformat{\chapter}[display]
    {\normalfont\centering\bfseries\fontsize{14pt}{17pt}\selectfont}
    {}
    {0pt}
    {\chaptername~\thechapter.\hspace{0.25em}}%
}

% --- Math & graphics ---
\usepackage{amsmath, amssymb, mathtools}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{booktabs}
\usepackage{svg}
\usepackage{graphicx}
\graphicspath{{images/}}

% --- Hyperlinks & bookmarks ---
\usepackage{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=blue,
  citecolor=black,
  pdftitle={Master thesis},
  pdfauthor={Roman Gorbunov}
}

% --- Bibliography (ГОСТ стиль) ---
\usepackage[style=gost-numeric, sorting=none, backend=biber]{biblatex}
\addbibresource{references.bib}