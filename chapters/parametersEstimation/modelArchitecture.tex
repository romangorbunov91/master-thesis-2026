\section{Архитектура модели}

Модель основана на автокодировщике, структурно состоящем из следующих основных блоков:
\begin{itemize}
    \item CNN-энкодер с residual-связями;
    \item bottleneck на основе алгоритма attention;
    \item ???-декодер;
    \item attention gated-skip-связи между энкодером и декодером;
    \item финальный слой.
\end{itemize}

На вход автоэнкодера подается многоканальный тензор длиной ${L}$:
\begin{equation*}
    {\mathbf{x}_{\text{in}} \in \mathbb{R}^{C_{\text{in}} \times L}},
\end{equation*}
где ${C_{\text{in}}}$ -- количество каналов.

На выходе формируется многоканальная бинарная маска.

\begin{equation*}
    {\mathbf{x}_{\text{out}} \in \mathbb{R}^{C_{\text{out}} \times L}}.
\end{equation*}

Количество каналов маски ${C_{\text{out}}}$ соответствует количеству детектируемых объектов.

\begin{figure}[H]
	\centering
	\includegraphics [width=0.85\linewidth] {parametersEstimation/model_block_diagram.drawio.png}

	\caption{Блок-схема архитектуры модели}
	\label{fig: model_block_diagram}
\end{figure}

\subsection{Энкодер}
Энкодер преобразует входные данные в латентное (скрытое) пространство уменьшенной размерности. В созданной реализации энкодер состоит из каскада $N$ сверточных базовых блоков. Заложена residual-архитектура базового блока по типу \enquote{pre-activation}.

\begin{figure}[H]
	\centering
	\includegraphics [width=0.25\linewidth] {parametersEstimation/encoder_block_diagram.drawio.png}

	\caption{Блок-схема базового блока энкодера}
	\label{fig: encoder_block_diagram}
\end{figure}

Каждый $n$-й базовы блок, ${n \in \{1, \dots, N\}}$, выполняет последовательно две свертки с ядрами ${\mathbf{W}_n^1}$, ${\mathbf{W}_n^2}$, соответственно.

\begin{equation*}
    \begin{aligned}
        \mathbf{z}_n^1 &= \mathbf{W}_n^1 \ast \sigma\left(\mathcal{N}\left(\mathbf{x}_{n-1}\right)\right), \\
        \mathbf{z}_n^2 &= \mathbf{W}_n^2 \ast \sigma\left(\mathcal{N}\left(\mathbf{z}_n^1\right)\right), \\
        \mathbf{x}_{n} &= \mathbf{z}_n^2 \oplus \mathbf{s_n},
    \end{aligned}
\end{equation*}
где ${\ast}$ -- операция сверкти, ${\oplus}$ -- поэлементная сумма.

Сквозная связь \enquote{вход-выход} $\mathbf{s_n}$ (skip-connection) базового слоя приводится к числу каналов (фичей) на выходе (при необходимости):
\begin{equation*}
    \mathbf{s_n} = 
        \begin{cases} 
            \mathbf{W}_n^s \ast \mathbf{x}_{n-1}, & \text{если} \quad F_{n-1} \neq F_{n} \\
            \mathbf{x}_{n-1}, & \text{иначе},
        \end{cases}
\end{equation*}
где ${\mathbf{W}_n^s \in \mathbb{R}^{F_{n} \times F_{n-1}}}$ -- ядро проекции.

Между слоями энкодера выполняется уменьшение размерности тензор в 2 раза алгоритмом \enquote{nn.MaxPool1d}:

\begin{equation*}
    \mathbf{x}_{n} \rightarrow \mathbf{x}_{n}{\downarrow_2}.
\end{equation*}

Таким образом, в энкодере выполняется последовательное увеличение числа каналов (фичей) с одновременным уменьшением размерности тензора. Residual-архитектура с pre-activation обеспечивают лучший градиентный поток для быстрой сходимости при обучении \cite{he2016identity}. Регуляризация выполняется алгоритмом \enquote{nn.Dropout1d} на выходе второй свертки.

\subsection{Bottleneck}
Bottleneck выделяет наиболее информативные признаки в скрытом пространстве. Реализован на алгоритме Multi-Head Self-Attention (MHSA).

С выхода энкодера поступает тензор ${\mathbf{e}_n \in \mathbb{R}^{F_3 \times L_3}}$. С помощью алгоритма \enquote{nn.MaxPool1d} длина тензора уменьшается до ${L_4}$, затем сверткой \enquote{nn.Conv1d} с ядром ${\mathbf{W}_\text{proj}}$ число каналов увеличивается до ${F_4}$:

\begin{equation*}
    \mathbf{b}_\text{proj} = \sigma\left(\mathcal{N}\left(\mathbf{W}_\text{proj} \ast \mathbf{e}_3{\downarrow_2}\right)\right),
\end{equation*}
где ${\mathbf{W}_\text{proj} \in \mathbb{R}^{F_4 \times F_3}}$


Вычисляются h-голов; для каждой головы ${m \in \{1, \dots, h\}}$: 

\begin{equation*}
    \begin{aligned}
        Q_m &= X W_m^Q \in \mathbb{R}^{L \times d_k} \\
        K_m &= X W_m^K \in \mathbb{R}^{L \times d_k} \\
        V_m &= X W_m^V \in \mathbb{R}^{L \times d_k} \\
        \text{Head}_m &= \text{Softmax}\left(\frac{Q_m K_m^\top}{\sqrt{d_k}}\right) V_m
    \end{aligned}
\end{equation*}

\begin{equation*}
    W_m^Q, W_m^K, W_m^V \in \mathbb{R}^{C \times d_{k}}
\end{equation*}
где ${d_{k}=C/h}$.

Слияние результатов вычислений:
\begin{equation*}
    \text{MHSA}(\mathbf{\hat{x}}) = \text{Concat}(\text{Head}_1, \dots, \text{Head}_h)\mathbf{W}^O
\end{equation*}

Residual connection:
\begin{equation*}
    \mathbf{x}' = \mathbf{x} \oplus \text{MHSA}(\mathbf{\hat{x}}).
\end{equation*}

Нормализация и MLP:
\begin{equation*}
    \text{MLP}(\mathbf{\tilde{x}}) = \text{Drop}\left(\mathbf{W}_2 \cdot \sigma_{\text{GELU}}\left(\mathbf{W}_1 \mathcal{N}_2(\mathbf{x}')\right)\right).
\end{equation*}

\begin{equation*}
    \begin{aligned}
        W_1 & \in \mathbb{R}^{(rC) \times C}, \\
        W_2 & \in \mathbb{R}^{C \times (rC)}.
    \end{aligned}
\end{equation*}
где $r$ - mlp-ratio.

Residual connection:
\begin{equation*}
    \mathbf{y} = \mathbf{x}' \oplus \text{MLP}(\mathbf{\tilde{x}})
\end{equation*}

\subsection{Декодер}
Каждый слой декодера поднимает разрешение массива. Далее свертка, нормализация, активация.

\begin{equation*}
    \begin{aligned}
        \mathbf{x}_{\uparrow} &= \text{Upsample}(\mathbf{x}, \text{scale}=s, \text{mode=linear}) \\
        \mathbf{z} &= \mathbf{W} \ast \mathbf{x}_{\uparrow} \\
        \mathbf{y} &= \sigma\left(\mathcal{N}(\mathbf{z})\right)
    \end{aligned}
\end{equation*}

\begin{equation*}
    \begin{aligned}
        &\textbf{Encoder:} \\
        \mathbf{e}_1 &= f_{\text{res}}^{(1)}(\mathbf{x}), \quad 
        \mathbf{e}_2 = f_{\text{res}}^{(2)}(\downarrow\mathbf{e}_1), \quad \dots \\
        \\
        &\textbf{Bottleneck:} \\
        \mathbf{b} &= f_{\text{trans}}\left(\sigma(\mathcal{N}(\mathbf{W}_{\text{proj}} \ast \downarrow\mathbf{e}_4))\right) \\
        \\
        &\textbf{Decoder (for } i=4,3,2,1\text{):} \\
        \mathbf{g}_i &= f_{\text{up}}(\mathbf{d}_{i+1}), \quad
        \tilde{\mathbf{e}}_i = f_{\text{att}}(\mathbf{g}_i, \mathbf{e}_i), \quad
        \mathbf{d}_i = f_{\text{res}}^{(i)}(\text{cat}(\mathbf{g}_i, \tilde{\mathbf{e}}_i)) \\
        \\
        &\textbf{Outputs:} \\
        \mathbf{y}_{\text{main}} &= \mathbf{W}_{\text{final}} \ast \mathbf{d}_1 \\
        \mathbf{y}_{\text{ds}}^{(k)} &= \mathbf{W}_{\text{ds}}^{(k)} \ast \mathbf{d}_{k+1} \quad \text{(training only)}
    \end{aligned}
\end{equation*}

\subsection{Attention-Gated Skip Connections}

Фичи энкодера перед слиянием с фичами декодера взвешиваются. По аналогии с %\cite{Attention_U_Net} https://arxiv.org/abs/1804.03999

Допустим, из skip-connection выходит

${\mathbf{x} \in \mathbb{R}^{B \times F_{\text{l}} \times L_{\text{x}}}}$.

Выход декодера

${\mathbf{g} \in \mathbb{R}^{B \times F_{\text{g}} \times L_{\text{g}}}}$.

Выход энкодера сначала согласуется по размерности - приводится к размерности декодера.

${\mathbf{x}' = \text{Interpolate}(\mathbf{x}, \text{size}=L_{\text{g}})}$.

Затем фичи через матрицы весовых коэффициентов ${W_{\text{g}}}$, ${W_{\text{x}}}$ согласуются по числу фичей (приводятся к ${F_\text{int}}$)
\begin{equation*}
    \begin{aligned}
        \mathbf{q} &= \mathcal{N}\left(\mathbf{W}_g \ast \mathbf{g}\right), \quad 
        \mathbf{k} &= \mathcal{N}\left(\mathbf{W}_x \ast \mathbf{x}'\right)
    \end{aligned}
\end{equation*}
где

${\mathbf{q}, \mathbf{k} \in \mathbb{R}^{B \times F_{\text{int}} \times L_{\text{g}}}}$.

\begin{equation*}
    W_g \in \mathbb{R}^{F_{\text{int}} \times F_g \times 1}, \quad
    W_x \in \mathbb{R}^{F_{\text{int}} \times F_l \times 1}, \quad
    W_\psi \in \mathbb{R}^{1 \times F_{\text{int}} \times 1}
\end{equation*}

Суммируются и проходят через функцию активации.

${\mathbf{a} = \sigma_{\text{ReLU}}\left(\mathbf{q} \oplus \mathbf{k}\right)}$.


Преобразуются к одному каналу
${\boldsymbol{\psi} = \sigma_{\text{Sigmoid}}\left(\mathcal{N}\left(\mathbf{W}_{\psi} \ast \mathbf{a}\right)\right)}$

${\boldsymbol{\psi} \in \mathbb{R}^{B \times 1 \times L_{\text{g}}}}$.
    
В декодер поступает 

${\mathbf{y} = \mathbf{x}' \odot \boldsymbol{\psi}}$

${\mathbf{y} \in \mathbb{R}^{B \times F_{\text{l}} \times L_{\text{g}}}}$


${\odot}$ - поэлементное умножение.

${\text{cat}(\cdot,\cdot)}$ - слияние по каналам.

${\downarrow_s}$ - Downsampling by factor $s$ (e.g., MaxPool).

${\uparrow_s}$ - Upsampling by factor $s$ (linear interpolation).